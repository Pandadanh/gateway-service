# Static Services Configuration (Optional Fallback)
# Chỉ dùng khi service chưa được register trong service registry
# Ưu tiên: Service Registry > Static Fallback

# Format: service-name: base-url
services:
  core: http://127.0.0.1:8081
  media: http://127.0.0.1:8082
  workflow: http://127.0.0.1:8089


